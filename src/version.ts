/**
 * The CLI's own version — single source for the update check and
 * `--version`. A test pins this to package.json so the two can never
 * drift; bump both together on release.
 */
export const VERSION = "0.9.1";
